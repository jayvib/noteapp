# Note this is only for Development
apiVersion: v1
kind: ConfigMap
metadata:
  name: noteapp-config
data:
  config.yaml: |
    store:
      file:
        path: /etc/noteapp
    server:
      port: 50001
